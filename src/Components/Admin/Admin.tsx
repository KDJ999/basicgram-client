import React from "react";


type AcceptedProps = {
  
  };
  
  type State = {
    // userData: UserDetails[];
    // results: UserDetails;
  };

export default class Admin extends React.Component<AcceptedProps, {}> {
    constructor(props: AcceptedProps) {
        super(props);
        this.state = {};
    }
    render() {
        return (
            <div>

            </div>
        )
    }
}